// NOTE: These are reference values from the backend schema, but the UI never uses them
// as fixed options. All filter chips and dropdowns are derived from actual DB data.
// See: src/types/index.ts on the backend for the authoritative definitions.
