services:
  closet:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ~/.closet:/data
    env_file:
      - .env
    environment:
      - CLOSET_DATA_DIR=/data
      - NODE_ENV=production
    restart: unless-stopped
